import { styled } from "styled-components";

const Wrapper = styled("div")({
  alignItems: "center",
  display: "flex",
  justifyContent: "space-around",
  marginBottom: "40px",
});

export const Styles = {
  Wrapper,
};